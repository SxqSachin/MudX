import { SkillData } from "@/types/Skill";
import { State } from "@/types/state";

const state: State = {
  id: "mental-shield",
  name: "金钟罩",
  stackable: false,
  remainTime: -1,
  actions: [
  ],
};

export default state;
